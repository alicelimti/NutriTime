# NutriTime 개발일지

> 영양제 복용 도우미 앱 — 개발 기록

---

## 2024-06-08 · Phase 1 MVP 개발 완료

### 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | NutriTime — 영양제 복용 도우미 |
| 슬로건 | 당신의 건강한 하루를 위한 스마트 영양제 관리 솔루션 |
| 기술 스택 | React 19 + Vite 8 + Firebase + localStorage |
| 배포 | GitHub Pages (`/NutriTime/`) |
| 저장소 | https://github.com/alicelimti/NutriTime |

---

### 작업 내용

#### 1. 프로젝트 초기 세팅
- GitHub 저장소 클론 (`alicelimti/NutriTime`)
- Vite + React 프로젝트 스캐폴딩
- Firebase SDK (v12) 설치 및 `src/firebase/config.js` 설정 파일 구성
- `.env.local` 환경변수 템플릿 생성 (`.gitignore`에 자동 제외)
- GitHub Pages 배포를 위한 `vite.config.js` base 경로 설정 (`/NutriTime/`)
- `gh-pages` 패키지 연동 (`npm run deploy`)

#### 2. 기획안 분석 (NutriTime.pdf)
PDF 기획안 9페이지를 분석하여 다음 6개 섹션의 요구사항 파악:

1. **앱 개요 및 핵심 가치** — 정확한 정보·스마트 알림·카카오 연동·건강 관리 4대 가치
2. **영양제 라이브러리** — 이모지·이름·효능 카드 UI, 상세 정보 모달
3. **복용 시간대 관리** — 기상직후~취침직전 5단계 그룹
4. **개인화 알림 시스템** — 30분 간격 설정, 복용 체크, 스마트 재알림
5. **카카오톡 알림 연동** — 플러스친구 메시지 발송 (Phase 2)
6. **수익화 전략** — iHerb·쿠팡·올리브영·GNC 제휴 (Phase 3)

#### 3. 파일 구조 설계 및 구현

```
src/
├── data/
│   └── supplements.js        # 영양제 15종 데이터 + 5단계 시간대 정의
├── components/
│   ├── NavBar.jsx / .css      # 하단 탭 네비게이션
│   └── SupplementModal.jsx / .css  # 영양제 상세 바텀시트 모달
├── pages/
│   ├── LibraryPage.jsx / .css    # 영양제 라이브러리 (그리드 + 검색)
│   ├── SchedulePage.jsx / .css   # 오늘의 복용 일정 (시간대별)
│   └── SettingsPage.jsx / .css   # 개인 설정 (시간 + 알림)
├── firebase/
│   └── config.js              # Firebase 초기화 (Firestore + Auth)
├── App.jsx                    # 전역 상태 관리 + 탭 라우팅
└── index.css                  # CSS 변수 / 글로벌 스타일
```

#### 4. 주요 기능 구현

**영양제 라이브러리**
- 15종 영양제 카드 그리드 (2열)
- 이름·효능 검색 필터
- `+` 버튼으로 기본 시간대에 즉시 추가 / 체크(✓) 표시로 제거
- 카드 탭 시 바텀시트 상세 모달 표시

**영양제 상세 모달**
- 상세 설명 / 좋은 조합 / 추천 연령대 / 복용 시간대 / 주의사항
- 하단 시간대 선택 버튼 — 탭하면 해당 그룹으로 이동

**내 일정 (복용 스케줄)**
- 5단계 시간대 그룹 (기상직후·오전·오후·저녁·취침직전)
- 오늘 복용 진행률 바 (체크수 / 전체)
- 각 영양제 체크 토글 (오늘 날짜 기준 초기화)
- 영양제 제거 버튼 (호버 시 노출)

**설정**
- 기상·아침·점심·저녁·취침 시간 `<input type="time">` 설정
- 푸시 알림 토글 UI
- 카카오톡 알림 연동 플레이스홀더 (Phase 2 예정)
- 제휴 파트너 섹션 (iHerb·쿠팡·올리브영·GNC)

**상태 관리**
- `localStorage` 기반 영구 저장 (영양제 일정 / 시간 설정 / 오늘 체크 내역)
- 오늘 체크 내역은 날짜 기준 자동 초기화

#### 5. 디자인 시스템

PDF 기획안 색상 그대로 적용:

| 변수 | 값 | 용도 |
|------|-----|------|
| `--primary` | `#3D3DC0` | 브랜드 인디고 |
| `--primary-dark` | `#2B2B9E` | 헤더 그라디언트 시작 |
| `--primary-light` | `#6B6BCF` | 헤더 그라디언트 끝 |
| `--primary-bg` | `#EEEEF8` | 카드 배경 라벤더 |
| `--accent` | `#7878D0` | 강조 요소 |

---

### 영양제 데이터 목록 (15종)

| # | 이름 | 효능 | 기본 시간대 |
|---|------|------|------------|
| 1 | 비타민D | 면역·뼈 강화 | 기상 직후 |
| 2 | 오메가3 | 혈관·뇌 건강 | 오전 |
| 3 | 마그네슘 | 피로·수면 개선 | 취침 직전 |
| 4 | 비타민C | 항산화·피부 | 오전 |
| 5 | 콜라겐 | 피부·관절 | 오전 |
| 6 | 유산균 | 장 건강·면역 | 기상 직후 |
| 7 | 비타민B군 | 에너지 대사 | 오후 |
| 8 | 코엔자임Q10 | 세포 에너지 | 오후 |
| 9 | 칼슘 | 뼈·치아 건강 | 저녁 |
| 10 | 비타민E | 항산화·혈액순환 | 저녁 |
| 11 | 멜라토닌 | 수면 개선 | 취침 직전 |
| 12 | 아연 | 면역·피부·성장 | 오전 |
| 13 | 철분 | 빈혈 예방·에너지 | 기상 직후 |
| 14 | 루테인 | 눈 건강 | 오전 |
| 15 | 비오틴 | 모발·피부·손톱 | 오전 |

---

### 다음 개발 계획

#### Phase 2 · 알림 고도화 (3~4개월차)
- [ ] Firebase Auth 연동 (회원가입 / 로그인)
- [ ] Firestore로 영양제 일정 동기화
- [ ] Web Push Notification API 연동
- [ ] 30분 간격 개인화 알림 설정
- [ ] 카카오톡 플러스친구 알림 연동 (Kakao Business API)
- [ ] 복용 이력 캘린더 뷰

#### Phase 3 · 수익화 (5~6개월차)
- [ ] iHerb 제휴 링크 연동
- [ ] 쿠팡 파트너스 API 연결
- [ ] 사용자 통계 대시보드
- [ ] 마케팅 캠페인 시작

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*NutriTime 기획안 v1.0 기반 · Phase 1 MVP 완료*

---

## 2026-06-08 · Phase 1.5 — 색상 테마 전면 개편

### 작업 배경

초기 인디고 계열 색상에서 건강·자연 이미지에 더 잘 맞는 컬러로 전면 개편.  
https://litt.ly/lottejobcafe_guide 사이트의 색상 조합 참고:

- 연두색 (`#E5FFD7`) — 메인 배경·강조 배경
- 흰 연두색 (`#F4FFF0`) — 앱 전체 기본 배경
- 노란 호버 (`#FDF165`) — 카드 호버 포인트 컬러

### 변경된 CSS 변수 (`src/index.css`)

| 변수 | 변경 전 | 변경 후 |
|------|---------|---------|
| `--primary` | `#3D3DC0` (인디고) | `#6DB343` (초록) |
| `--primary-dark` | `#2B2B9E` | `#4A9E26` |
| `--primary-light` | `#6B6BCF` | `#9AD96A` |
| `--primary-bg` | `#EEEEF8` (라벤더) | `#E5FFD7` (연두) |
| `--accent` | `#7878D0` | `#FDF165` (노란) |
| `--text-primary` | `#1A1A5E` | `#1A4008` |
| `--text-secondary` | `#4A4AAA` | `#4A7D2A` |
| `--bg` | `#F5F5FB` | `#F4FFF0` |
| `body` 배경 | `#E8E8F5` | `#C0E8A0` |

### 기술 이슈 및 해결

**초기 배포 시 백지 화면 문제**
- 원인: `height: 100%` 체인(html→body→#root→.app)이 일부 브라우저에서 0px로 계산됨
- 해결: `.app`에 `min-height: 100vh; min-height: 100dvh` 적용,  
  `.page-content`에 `min-height: 0` 추가
- JSDOM 헤드리스 렌더 테스트로 JS 정상 실행 확인 후 CSS 원인 특정

**GitHub Pages 브랜치 설정 오류**
- 원인: Pages가 `main` 브랜치 루트(`src="/src/main.jsx"` 포함된 개발용 index.html)를 서빙
- 해결: GitHub REST API (`PUT /repos/alicelimti/NutriTime/pages`)로 소스 브랜치를 `gh-pages`로 변경

---

## 2026-06-09 · Phase 1.6 — 복용약 라이브러리 탭 추가

### 작업 배경

영양제 라이브러리만으로는 실제 처방약을 복용하는 성인 사용자의 니즈를 충족하기 어려움.  
혈압약·당뇨약 등 흔한 성인병 처방약을 별도 탭으로 분리하여 체계적으로 관리할 수 있도록 기능 추가.

### 추가된 파일

```
src/
├── data/
│   └── medications.js          # 처방약 15종 데이터
├── components/
│   └── MedicationModal.jsx     # 복용약 상세 바텀시트 모달 (SupplementModal.css 공유)
└── pages/
    ├── MedicationPage.jsx      # 복용약 라이브러리 (그리드 + 검색)
    └── MedicationPage.css      # 파란 계열 헤더, 카드 스타일 공유
```

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/NavBar.jsx` | 4탭 구성으로 변경 (라이브러리 → 복용약 → 내일정 → 설정) |
| `src/App.jsx` | `myMedications` 상태 추가, `toggleMedication`·`isInMedSchedule` 함수 구현 |
| `src/pages/SchedulePage.jsx` | 복용약을 시간대 그룹에 병렬 표시, 파란 '처방약' 배지 구분 |
| `src/pages/SchedulePage.css` | `.med-item`·`.med-badge` 스타일 추가 |

### 처방약 데이터 목록 (15종)

| # | 이름 | 효능 | 기본 시간대 |
|---|------|------|------------|
| 1 | 혈압약 | 고혈압 관리 | 오전 |
| 2 | 당뇨약 | 혈당 조절 | 오전 |
| 3 | 고지혈증약 | 콜레스테롤 조절 | 취침 직전 |
| 4 | 갑상선약 | 갑상선 호르몬 보충 | 기상 직후 |
| 5 | 위산억제제 | 위궤양·역류성식도염 치료 | 오전 |
| 6 | 통풍약 | 요산 수치 조절 | 오전 |
| 7 | 골다공증약 | 뼈 강화·골절 예방 | 기상 직후 |
| 8 | 전립선비대증약 | 배뇨 개선 | 취침 직전 |
| 9 | 아스피린 | 혈전 예방·심혈관 보호 | 오전 |
| 10 | 협심증약 | 심장 혈류 개선 | 오전 |
| 11 | 천식·COPD 흡입제 | 기관지 확장·호흡 개선 | 오전 |
| 12 | 항우울제 | 우울·불안 증상 완화 | 오전 |
| 13 | 수면제 | 수면 유도·불면 치료 | 취침 직전 |
| 14 | 치매 치료제 | 인지 기능 보호·진행 억제 | 저녁 |
| 15 | 관절염약 | 관절 통증·염증 완화 | 오전 |

각 약마다 **약물 설명 / 복용 방법 / 복용 시간대 / 주요 부작용 / 약물 상호작용 / 주요 대상** 6가지 정보 수록.

### 설계 결정

- **영양제와 복용약의 checked 상태 공유**: `todayChecked` 배열에 ID 문자열로 구분 (영양제는 `vitd` 등, 처방약은 `med_` 접두어). 단일 배열로 관리해 복잡도 최소화.
- **내 일정 통합 표시**: 영양제와 처방약을 같은 시간대 그룹 내에 표시. 파란 '처방약' 배지로 시각 구분.
- **MedicationModal**: `SupplementModal.css` 재사용으로 일관된 바텀시트 디자인 유지.
- **파란 헤더**: 복용약 페이지 헤더를 파란 계열 그라디언트(`#3D7DB8→#5294CC→#7AB8E8`)로 처리해 영양제(초록)와 구분.

### 상태 관리 구조 (최종)

```
localStorage
├── mySupplements   { wake:[], morning:[], afternoon:[], evening:[], bedtime:[] }
├── myMedications   { wake:[], morning:[], afternoon:[], evening:[], bedtime:[] }
├── settings        { wakeTime, breakfastTime, lunchTime, dinnerTime, bedtimeTime }
└── todayChecked    { date: "...", checked: ["vitd", "med_bp", ...] }
```

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 1.6 · 영양제(15종) + 처방약(15종) 라이브러리 완성*

---

## 2026-06-11 · Phase 1.7 — 하단 탭 레이블 수정 + 음수량 탭 신규 추가

### 작업 배경

영양제 라이브러리 탭 레이블이 "라이브러리"로 표시되어 내용과 불일치.  
또한 건강 관리 앱으로서 음수량 추적 기능이 누락되어 있어 하루 물 섭취량을 기록할 수 있는 탭 신규 추가.

---

### 1. 하단 탭 레이블 수정

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/NavBar.jsx` | 첫 번째 탭 레이블 `'라이브러리'` → `'영양제'` 로 변경 |

---

### 2. 음수량 탭 신규 추가

#### 추가된 파일

```
src/pages/
├── WaterPage.jsx    # 음수량 페이지 메인 컴포넌트
└── WaterPage.css    # 파란 계열 테마 스타일
```

#### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/NavBar.jsx` | 5탭 구성으로 변경 — 복용약 다음에 `💧 음수량` 탭 삽입 |
| `src/App.jsx` | `WaterPage` import 및 `tab === 'water'` 라우팅 추가 |

---

#### 주요 기능

**체중 기반 권장 음수량 계산**
- 상단 카드에 체중(kg) 입력 → `체중 × 30ml` 공식으로 권장 음수량 즉시 계산
- 체중값은 `localStorage`에 영구 저장

**SVG 투명 물병 시각화**
- 순수 SVG로 구현한 유리 물병 모양 (네크 + 어깨 곡선 + 몸통 + 둥근 바닥)
- 글래스 효과: 반투명 테두리(`rgba(100,181,246,0.65)`), 내부 세로 하이라이트 shine
- 물 채워짐: 하단에서 위로 애니메이션 (`cubic-bezier(.34,1.56,.64,1)` 스프링 효과)
- 수면 파도 애니메이션: SVG `<animateTransform>` 으로 타원이 좌우로 흔들림
- 100ml 단위 눈금 틱 마크, 500ml 단위 레이블(1L 형식) 표시
- 물병 최상단에 목표 음수량 배지(`목표 Nml`) 항상 노출
- 물 채워진 비율이 18% 초과 시 물병 내부에 현재 음수량 텍스트 표시

**음수량 기록 조작** *(초기 구현 — 이후 Range Slider로 교체됨)*
- `+100ml` / `-100ml` 버튼으로 100ml 단위 기록
- 원형 진행률 SVG 그래프로 달성률(%) 중앙 표시
- 초기화 버튼 (소비량 > 0 일 때만 노출)
- 목표 달성 시 🎉 팝인 애니메이션 배너 표시

**상태 관리**
- 음수량은 오늘 날짜(`toDateString()`) 기준 자동 초기화
- 체중은 날짜 무관 유지

#### 상태 관리 구조 (최종)

```
localStorage
├── mySupplements   { wake:[], morning:[], afternoon:[], evening:[], bedtime:[] }
├── myMedications   { wake:[], morning:[], afternoon:[], evening:[], bedtime:[] }
├── settings        { wakeTime, breakfastTime, lunchTime, dinnerTime, bedtimeTime }
├── todayChecked    { date: "...", checked: ["vitd", "med_bp", ...] }
├── waterWeight     "65"  (체중, 문자열)
└── waterConsumed   { date: "...", amount: 1500 }
```

#### 디자인 결정

- **파란 계열 헤더**: 음수량 페이지 헤더를 파란 그라디언트(`#0D47A1 → #1976D2 → #42A5F5`)로 처리. 영양제(초록) · 복용약(하늘파랑)과 구분되는 짙은 파랑으로 물 이미지 강조.
- **SVG 선택 이유**: CSS만으로는 어깨 곡선이 있는 병 모양의 clipPath 구현이 어려움. SVG `<path>` + `<clipPath>`로 정확한 병 모양 안에 물 채움 효과 구현.
- **5탭 레이아웃**: 탭이 4개 → 5개로 늘어나 각 탭의 가로 폭이 줄어들지만, `font-size: 11px` 레이블과 `22px` 아이콘으로 480px 최대 너비 내에서 균형 유지.

---

## 2026-06-11 · Phase 1.7.1 — 음수량 입력 UI를 세로 Range Slider로 교체

### 작업 배경

버튼(+100ml / -100ml) 방식은 100ml 단위로만 입력 가능해 세밀한 기록이 불가.  
맥북 볼륨 조절처럼 드래그로 연속적으로 조절하는 아날로그식 UI로 개선 요청.

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/pages/WaterPage.jsx` | 버튼·원형 링 제거 → 세로 슬라이더 + `bottle-and-slider` 레이아웃으로 교체 |
| `src/pages/WaterPage.css` | 슬라이더 전용 스타일 추가, 버튼 관련 스타일 전면 제거 |

### 구현 방식

**세로 슬라이더 CSS 기법**
- `writing-mode: vertical-lr; direction: rtl` — 세로 방향 슬라이더, 위=최대값·아래=0
- `step="10"` / `min="0"` / `max="목표량(미설정 시 3000ml)"` — 10ml 단위 연속 조절
- CSS custom property `--fill-pct`를 inline style로 주입 → `::-webkit-slider-runnable-track` background 그라디언트 실시간 갱신
- `linear-gradient(to top, #1976D2 var(--fill-pct), #DDEEFF var(--fill-pct))` — 채워진 구간(파랑)·빈 구간(연한 파랑) 실시간 표현

**크로스 브라우저 대응**
| 엔진 | 사용한 pseudo-element |
|------|-----------------------|
| Chrome / Safari | `::-webkit-slider-thumb`, `::-webkit-slider-runnable-track` |
| Firefox | `::-moz-range-thumb`, `::-moz-range-track`, `::-moz-range-progress` |

**thumb 디자인**
- 흰 원형(28px) + 파란 테두리(`border: 3px solid #1976D2`) + 그림자
- `:active` 시 `scale(1.2)` + 그림자 강조 → 잡는 피드백

**레이아웃 변경**
- `bottle-and-slider` flex row: 물병 SVG(120px) + 슬라이더 컬럼(8px track, 264px 높이)
- 슬라이더 최상단 목표량 레이블 / 최하단 `0` 레이블
- 원형 진행 링 제거 → 하단 `현재량ml / 목표량ml N%` 텍스트 한 줄로 단순화
- 물 채움 transition을 `0.55s → 0.25s`로 단축해 드래그 응답성 개선

### 디자인 결정

- **writing-mode 선택 이유**: `transform: rotate(-90deg)` 방식은 컨테이너 크기 계산이 틀어지고 터치 이벤트 방향도 뒤집힘. `writing-mode: vertical-lr`은 브라우저가 레이아웃·이벤트를 모두 올바르게 처리.
- **track 배경 vs ::-webkit-slider-runnable-track**: `::-webkit-slider-runnable-track`에 `background: transparent`을 주고 요소 자체의 background에 그라디언트를 적용하면 CSS custom property 값이 pseudo-element까지 상속되어 실시간 업데이트가 가능.

---

## 2026-06-15 · Phase 1.8 — 영양제·복용약 목록 가나다 순 정렬

### 작업 배경

영양제·복용약 목록이 추가 순서대로 나열되어 있어 원하는 항목을 찾기 불편.  
한국어 사전 순(Unicode Hangul 초성→중성→종성 기준)으로 전면 재정렬.

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/data/supplements.js` | `SUPPLEMENTS` 배열 16종 가나다 순 재배열 |
| `src/data/medications.js` | `MEDICATIONS` 배열 15종 가나다 순 재배열 |

### 정렬 결과

**영양제 (16종)**

| 순서 | 이름 | 비고 |
|------|------|------|
| 1 | 루테인 | ㄹ |
| 2 | 마그네슘 | ㅁ |
| 3 | 멜라토닌 | ㅁ (마<멜: ㅏ<ㅔ) |
| 4 | 비오틴 | ㅂ (비오<비타: 초성 ㅇ<ㅌ) |
| 5 | 비타민B군 | ㅂ |
| 6 | 비타민C | ㅂ |
| 7 | 비타민D | ㅂ |
| 8 | 비타민E | ㅂ |
| 9 | 아연 | ㅇ |
| 10 | 오메가3 | ㅇ (아<오: ㅏ<ㅗ) |
| 11 | 유산균 | ㅇ (오<유: ㅗ<ㅜ) |
| 12 | 철분 | ㅊ |
| 13 | 칼슘 | ㅋ |
| 14 | 코엔자임Q10 | ㅋ (칼<코: ㅏ<ㅗ) |
| 15 | 콜라겐 | ㅋ (코<콜: 받침 없음<ㄹ) |
| 16 | 크랜베리 | ㅋ (콜<크: ㅗ<ㅡ) |

**복용약 (15종)**

| 순서 | 이름 | 비고 |
|------|------|------|
| 1 | 갑상선약 | ㄱ (갑: ㅏ) |
| 2 | 고지혈증약 | ㄱ (고: ㅗ, 받침 없음) |
| 3 | 관절염약 | ㄱ (관: ㅗ+ㄴ받침) |
| 4 | 골다공증약 | ㄱ (골: ㅗ+ㄹ받침) |
| 5 | 당뇨약 | ㄷ |
| 6 | 수면제 | ㅅ |
| 7 | 아스피린 | ㅇ (아: ㅏ) |
| 8 | 위산억제제 | ㅇ (위: ㅟ) |
| 9 | 전립선비대증약 | ㅈ |
| 10 | 천식·COPD 흡입제 | ㅊ (천: ㅔ) |
| 11 | 치매 치료제 | ㅊ (치: ㅣ) |
| 12 | 통풍약 | ㅌ |
| 13 | 항우울제 | ㅎ (항: ㅏ) |
| 14 | 혈압약 | ㅎ (혈: ㅕ+ㄹ받침) |
| 15 | 협심증약 | ㅎ (협: ㅕ+ㅂ받침) |

### 정렬 기준

Unicode Hangul 음절 코드포인트 순서를 따름:
- **초성(자음)**: ㄱ ㄴ ㄷ ㄹ ㅁ ㅂ ㅅ ㅇ ㅈ ㅊ ㅋ ㅌ ㅍ ㅎ
- **중성(모음)**: ㅏ ㅐ ㅑ ㅒ ㅓ ㅔ ㅕ ㅖ ㅗ … ㅜ … ㅡ … ㅣ
- **종성(받침)**: 없음 < ㄴ < ㄹ < ㅂ … 순

---

## 2026-06-15 · 데이터 정리 — 영양제·복용약 항목 삭제

### 변경 내용

| 파일 | 삭제 항목 | 잔여 종수 |
|------|-----------|-----------|
| `src/data/supplements.js` | 비오틴, 콜라겐 | 16종 → **14종** |
| `src/data/medications.js` | 골다공증약, 전립선비대증약, 천식·COPD 흡입제, 치매 치료제 | 15종 → **11종** |

### 현재 목록

**영양제 (14종)**
루테인 · 마그네슘 · 멜라토닌 · 비타민B군 · 비타민C · 비타민D · 비타민E · 아연 · 오메가3 · 유산균 · 철분 · 칼슘 · 코엔자임Q10 · 크랜베리

**복용약 (11종)**
갑상선약 · 고지혈증약 · 관절염약 · 당뇨약 · 수면제 · 아스피린 · 위산억제제 · 통풍약 · 항우울제 · 혈압약 · 협심증약

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 1.8 · 영양제(14종) + 복용약(11종)*

---

## 2026-06-15 · 데이터 추가 — 종합비타민 영양제 신규 추가

### 변경 내용

| 파일 | 추가 항목 | 변경 종수 |
|------|-----------|-----------|
| `src/data/supplements.js` | 종합비타민 | 14종 → **15종** |

### 배치 위치

가나다 순 기준 ㅈ 초성 — 유산균(ㅇ)과 철분(ㅊ) 사이(11번째)에 삽입.

### 종합비타민 데이터

| 항목 | 내용 |
|------|------|
| ID | `multivitamin` |
| 이모지 | 🌈 |
| 효능 | 전반적인 영양 보충 |
| 기본 시간대 | 오전 (아침 식후) |
| 주의사항 | 개별 비타민과 중복 복용 시 지용성 비타민(A·D·E·K) 과잉 섭취 주의 |

### 현재 목록

**영양제 (15종)**
루테인 · 마그네슘 · 멜라토닌 · 비타민B군 · 비타민C · 비타민D · 비타민E · 아연 · 오메가3 · 유산균 · 종합비타민 · 철분 · 칼슘 · 코엔자임Q10 · 크랜베리

**복용약 (11종)**
갑상선약 · 고지혈증약 · 관절염약 · 당뇨약 · 수면제 · 아스피린 · 위산억제제 · 통풍약 · 항우울제 · 혈압약 · 협심증약

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 1.8 · 영양제(15종) + 복용약(11종)*

---

## 2026-06-15 · Phase 1.9 — 탭별 헤더 색상 차별화

### 작업 배경

모든 탭의 상단 헤더가 동일한 초록 계열로 표시되어 탭 전환 시 시각적 구분이 부족.  
사용자가 제공한 이미지(롯데호텔 부산 안내 페이지)의 테마 컬러를 추출해 탭마다 고유한 헤더 색상 적용.

### 이미지에서 추출한 컬러

| 색상 요소 | 근사값 | 활용 |
|-----------|--------|------|
| 사이드 세이지 그린 | `#8EC860` | 내 일정·설정 계열 기준 |
| 웜 피치 카드 | `#FAE8D0` | 복용약 계열 기준 |

### 탭별 헤더 색상

| 탭 | 그라디언트 | 비고 |
|----|-----------|------|
| 🌿 영양제 | `#4A9E26 → #6DB343 → #9AD96A` | 기존 유지 |
| 💊 복용약 | `#A85C28 → #C47840 → #E09A60` | 웜 앰버/테라코타 — 피치 카드에서 파생 |
| 💧 음수량 | `#0D47A1 → #1976D2 → #42A5F5` | 기존 유지 |
| 📋 내 일정 | `#1A6860 → #2A8878 → #48AEA0` | 티얼/옥빛 — 세이지 그린 보색 계열 |
| ⚙️ 설정 | `#4A5A28 → #6A7A40 → #8A9A60` | 올리브/모스 — 세이지 그린 어두운 톤 |

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/pages/MedicationPage.css` | `.medication-header` 그라디언트 교체 |
| `src/pages/SchedulePage.css` | `.schedule-header` 그라디언트 교체 |
| `src/pages/SettingsPage.css` | `.settings-header` 그라디언트 교체 |

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 1.9 · 탭별 고유 헤더 색상 적용 완료*

---

## 2026-06-19 · Phase 2.0 — 5개 탭 헤더 높이 통일

### 작업 배경

탭마다 헤더 내부 요소 수가 달라(검색바 유무, 진행률 표시 유무) 탭 전환 시 콘텐츠 시작 위치가 들쭉날쭉했음.  
영양제 탭의 헤더를 기준 높이로 삼아 나머지 4개 탭을 맞춤.

### 헤더 높이 분석

| 탭 | 헤더 내 요소 | 기존 높이(추정) | 변경 전 padding-bottom | 변경 후 padding-bottom |
|----|-------------|----------------|----------------------|----------------------|
| 🌿 영양제 | 타이틀 + 서브 + 검색바 | ~153px | 24px | 24px (기준, 유지) |
| 💉 복용약 | 타이틀 + 서브 + 검색바 | ~153px | 24px | 24px (동일 구조, 유지) |
| 💧 음수량 | 타이틀 + 서브 | ~101px | 24px | **76px** (+52px) |
| 📋 내 일정 | 타이틀 + 진행률 바 + 텍스트 | ~127px | 24px | **50px** (+26px) |
| ⚙️ 설정 | 타이틀 + 서브 | ~101px | 24px | **76px** (+52px) |

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/pages/WaterPage.css` | `.water-header` padding-bottom `24px → 76px` |
| `src/pages/SchedulePage.css` | `.schedule-header` padding-bottom `24px → 50px` |
| `src/pages/SettingsPage.css` | `.settings-header` padding-bottom `24px → 76px` |

### 설계 결정

- **padding-bottom 조정 방식 선택 이유**: `min-height` 고정 대신 padding을 늘려 내용이 추가되더라도 자연스럽게 헤더가 늘어나도록 함.
- **복용약 수정 불필요**: 영양제와 완전히 동일한 DOM 구조(타이틀 + 서브 + 검색바)라 이미 같은 높이.

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 2.0 · 5개 탭 헤더 높이 통일 완료*

---

## 2026-06-19 · Phase 2.1 — 탭 헤더 파스텔 컬러 테마 적용

### 작업 배경

기존 헤더 색상이 짙은 다크 그라디언트 계열이어서 무거운 느낌.  
사용자 정의 파스텔 팔레트(CSS 변수로 정의)를 적용해 부드럽고 따뜻한 헤더 분위기로 전환.

### 적용 색상 팔레트

```css
--color-butter-supplement: #F5D99D;  /* 🌿 영양제 */
--color-mint-medicine:     #A8D5C4;  /* 💉 복용약 */
--color-blue-negative:     #C8D7F0;  /* 💧 음수량 */
--color-pink-mytab:        #EFD4E5;  /* 📋 내 일정 */
--color-ivory-settings:    #F9F7F2;  /* ⚙️ 설정 */
```

### 탭별 적용 결과

| 탭 | 그라디언트 | 텍스트 |
|----|-----------|--------|
| 🌿 영양제 | `#EBC776 → #F5D99D → #FAE9C0` | `#6B4800` (다크 앰버) |
| 💉 복용약 | `#80C4AF → #A8D5C4 → #C8E8DF` | `#1A3D34` (다크 틸) |
| 💧 음수량 | `#A5BEE4 → #C8D7F0 → #DDE9F8` | `#1A2A52` (다크 네이비) |
| 📋 내 일정 | `#E4BCD8 → #EFD4E5 → #F6EAF3` | `#52173A` (다크 로즈) |
| ⚙️ 설정 | `#EAE4D6 → #F1EBE0 → #F9F7F2` | `#2E2820` (다크 브라운) |

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/pages/LibraryPage.css` | 배경 버터 그라디언트, 텍스트·검색바 다크 앰버 |
| `src/pages/MedicationPage.css` | 배경 민트 그라디언트, 텍스트·검색바 다크 틸 |
| `src/pages/WaterPage.css` | 배경 라이트블루 그라디언트, 텍스트 다크 네이비 |
| `src/pages/SchedulePage.css` | 배경 핑크 그라디언트, 텍스트·진행바 다크 로즈 |
| `src/pages/SettingsPage.css` | 배경 아이보리 그라디언트, 텍스트 다크 브라운 |

### 설계 결정

- **배경이 밝아짐에 따른 전체 텍스트 색 전환**: 기존 흰색 텍스트 계열(`#fff`, `rgba(255,255,255,0.75)`)을 각 테마에 맞는 어두운 색으로 일괄 교체. 가독성 및 명암비(WCAG) 확보.
- **장식 원형 변경**: `rgba(255,255,255,0.07)` → `rgba(0,0,0,0.05)` — 밝은 배경에서 흰 원형은 보이지 않으므로 어두운 반투명으로 전환.
- **검색바**: `rgba(255,255,255,0.18)` → `rgba(0,0,0,0.07)` — 파스텔 배경 위에서 자연스러운 구분감 유지.
- **내 일정 진행바**: fill 색을 흰색 → `#C05880`(딥 로즈)으로 변경해 핑크 배경과 대비 확보.

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 2.1 · 파스텔 테마 헤더 완성*

---

## 2026-06-19 · Phase 3.0 — Supabase 마이그레이션 + PWA 전환

### 작업 배경

Firebase → Supabase로 백엔드를 교체하고, PWA(Progressive Web App)로 전환해 홈화면 설치 및 오프라인 지원을 추가했다.  
최종적으로는 강제 로그인 없이 앱을 사용하고, 설정 탭에서 선택적으로 로그인할 수 있는 구조로 정착했다.

---

### Phase 3.0 — Supabase 마이그레이션

#### Firebase 제거

| 작업 | 내용 |
|------|------|
| `npm uninstall firebase` | firebase 패키지 84개 제거 |
| `src/firebase/config.js` 삭제 | Firebase 초기화 파일 제거 |
| `.env.local` 교체 | `VITE_FIREBASE_*` → `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` |

#### Supabase 설치 및 설정

```bash
npm install @supabase/supabase-js  # v2.108.2
```

**생성 파일:**

| 파일 | 역할 |
|------|------|
| `src/supabaseClient.js` | Supabase 클라이언트 초기화 |
| `src/services/authService.js` | Google OAuth 로그인/로그아웃/세션 감지 |
| `src/services/medicationService.js` | 복용약 CRUD + 복용 기록 |
| `src/services/supplementService.js` | 영양제 CRUD + 복용 기록 |
| `src/services/waterService.js` | 음수량 기록 + 목표 관리 |

---

### Phase 3.1 — PWA 전환

#### 생성 파일

| 파일 | 역할 |
|------|------|
| `public/manifest.json` | 앱 이름·테마색(`#22c55e`)·아이콘 경로 |
| `public/service-worker.js` | 오프라인 캐싱 (Stale-While-Revalidate) |
| `public/icons/icon.svg` | NT 알약 디자인 SVG 아이콘 |
| `public/icons/icon-192x192.png` | 무병장수 로고 (홈화면 설치 아이콘) |
| `public/icons/icon-512x512.png` | 무병장수 로고 대형 |
| `scripts/generate-icons.mjs` | sharp 기반 PNG 생성 스크립트 |

#### index.html 추가 메타태그

```html
<link rel="manifest" href="/NutriTime/manifest.json" />
<meta name="theme-color" content="#22c55e" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<link rel="apple-touch-icon" href="/NutriTime/icons/icon-192x192.png" />
```

#### Service Worker 전략

- **Supabase API / Google OAuth** → 캐싱 제외 (항상 네트워크)
- **정적 자산** → Stale-While-Revalidate (캐시 즉시 반환 + 백그라운드 갱신)
- **오프라인** → 캐시된 버전 제공

---

### Phase 3.2 — 로그인 화면 + 메인 허브 (시도 후 롤백)

#### 시도한 구조

- `LoginPage.jsx` — Google OAuth 로그인 화면 (강제 진입)
- `MainPage.jsx` — 4개 파스텔 카드 허브 화면
- 앱 진입 흐름: 로그인 → MainPage → 각 탭

#### 롤백 이유

사용자가 오전 9시 기준 기존 UI(카드 그리드 + 검색 + 상세 모달)를 선호해  
강제 로그인 및 MainPage 허브를 제거하고 원래 UI로 복원.

---

### Phase 3.3 — 최종 구조 확정

#### 앱 진입 방식

- 로그인 없이 앱 바로 진입 (기존과 동일)
- Supabase 세션은 백그라운드에서 감지만 함

#### NavBar — 5탭 (9AM 원본 복원)

| 탭 | 페이지 |
|----|--------|
| 🌿 영양제 | LibraryPage — 카드 그리드 + 검색 + 상세 모달 |
| 💉 복용약 | MedicationPage — 카드 그리드 + 검색 + 상세 모달 |
| 💧 음수량 | WaterPage — Range Slider |
| 📋 내 일정 | SchedulePage — 시간대별 복용 스케줄 |
| ⚙️ 설정 | SettingsPage — 시간 설정 + **로그인 통합** |

#### 설정 탭 변경 사항

| 섹션 | 변경 |
|------|------|
| ⏰ 나의 하루 일정 | 유지 |
| 🔔 알림 설정 | 유지 |
| 🛍️ 제휴 파트너 | **삭제** |
| 👤 계정 | **신규** — 미로그인 시 Google 로그인 버튼 / 로그인 시 프로필 + 로그아웃 |
| ℹ️ 앱 정보 | 유지 |

---

### OAuth 트러블슈팅

| 오류 | 원인 | 해결 |
|------|------|------|
| `redirect_uri_mismatch` | Google Cloud Console Redirect URI 미등록 | `https://pgnxvsbsxsrvvgnggvks.supabase.co/auth/v1/callback` 추가 |
| `localhost:3000` 리다이렉트 | Supabase Site URL이 localhost | Site URL → `https://alicelimti.github.io/NutriTime/` 변경 |

---

### 커밋 이력

| 커밋 | 내용 |
|------|------|
| `7b25440` | feat: Supabase 연동 + PWA 전환 (Phase 3) |
| `474c9c2` | revert: 이전 UI 복원 (카드 그리드 + 검색 + 모달) |
| `383b898` | style: 홈화면 앱 아이콘 교체 (무병장수 로고) |
| `7cb2ff8` | feat: 9AM UI 복원 + My 탭에 로그인 통합 |
| `423b0a7` | refactor: 설정 + My 탭 통합 |

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 3.3 · Supabase + PWA · 설정 탭 로그인 통합 완성*

---

## 2026-06-19 · Phase 3.4 — 하단 탭 크기 확대 + 계정 섹션 여백 통일

### 작업 배경

레퍼런스 앱과 비교했을 때 하단 탭 높이가 작고, 설정 탭 계정 섹션의 여백이 다른 행들과 불일치.

### 변경 내용

| 커밋 | 파일 | 변경 내용 |
|------|------|-----------|
| `f997a4f` | `public/service-worker.js` | CACHE_NAME `nutritime-v1` → `nutritime-v2` (stale 캐시 강제 교체) |
| `806de43` | `src/components/NavBar.css` | 탭 높이 확대 (레퍼런스 앱 기준) |
| `806de43` | `src/pages/SettingsPage.css` | `.settings-profile-row` padding을 `14px 16px`로 변경해 푸시알림 행과 통일 |
| `806de43` | `src/pages/SettingsPage.css` | `.settings-signout-btn`, `.settings-google-btn` 좌우 마진 `16px`으로 통일 |

### 이슈

서비스 워커 캐시 버전 업(`v2`)만으로는 흰 화면이 완전히 해결되지 않음 → Phase 3.5에서 근본 원인 수정.

---

## 2026-06-19 · Phase 3.5 — 서비스 워커 전략 전면 개편 (흰 화면 1차 수정)

### 문제 원인

이전 SW가 `index.html`을 Stale-While-Revalidate로 캐싱 → 새 배포 후에도 구버전 `index.html`(구버전 asset hash 포함)을 반환 → 새 hash로 빌드된 JS/CSS를 찾지 못해 흰 화면 발생.

### 해결 방법

Navigate 요청(HTML 탐색)은 항상 네트워크 퍼스트로 처리. 정적 자산(`/assets/`)은 해시 파일명이 불변이므로 캐시 퍼스트 유지.

### 변경된 파일 (`public/service-worker.js`)

```js
const CACHE_NAME = 'nutritime-v3'

// navigate 요청 → 항상 네트워크, 실패 시 캐시 폴백
if (request.mode === 'navigate') {
  return fetch(request).catch(() => caches.match(request))
}
// /assets/ → 캐시 퍼스트 (해시 파일명으로 불변 보장)
if (url.pathname.includes('/assets/')) {
  return caches.match(request).then(cached => cached || fetchAndCache(request))
}
```

### 설계 결정

- **navigate 분리 이유**: `index.html`은 매 배포마다 내용이 바뀌지만 URL은 고정. Stale-While-Revalidate는 URL 기반 캐싱이라 구버전 HTML을 계속 반환하는 구조적 문제가 있음.
- **`/assets/` 캐시 퍼스트 유지**: Vite가 빌드 시 파일명에 content hash를 포함시키므로 내용이 바뀌면 URL 자체가 바뀜 → 구버전 캐시가 남아도 새 파일에 히트하지 않아 안전.

---

## 2026-06-19 · Phase 3.6 — 앱 아이콘 풀 블리드 재생성

### 문제

홈화면 아이콘에 흰색 여백(corner gap)이 보임. iOS는 아이콘에 자체 마스크(라운드 코너)를 씌우는데, 기존 PNG에 둥근 모서리와 흰 배경이 이미 포함되어 있어 이중 처리됨.

### 해결

Python PIL로 라운드 코너 없이 정사각형 풀 블리드 PNG를 재생성. iOS가 마스크를 직접 적용해 깔끔하게 표시됨.

```python
# 대각선 그라디언트 + "무병장수" 텍스트
# AppleSDGothicNeo.ttc 시스템 폰트 사용
img = Image.new('RGB', (512, 512))
draw = ImageDraw.Draw(img)
# 픽셀 단위 대각선 그라디언트 (#6DB343 → #EBC776)
for y in range(512):
    for x in range(512):
        t = (x + y) / (512 * 2)
        draw.point((x, y), color=lerp(start_color, end_color, t))
```

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `public/icons/icon-180x180.png` | 풀 블리드 재생성 |
| `public/icons/icon-192x192.png` | 풀 블리드 재생성 |
| `public/icons/icon-512x512.png` | 풀 블리드 재생성 |

---

## 2026-06-19 · Phase 3.7 — 하단 탭 이모지 → SVG 아이콘

### 작업 배경

이모지 아이콘은 플랫폼별로 렌더링이 달라 디자인 일관성이 떨어짐. 레퍼런스 앱을 참고해 stroke 스타일 SVG 아이콘으로 교체하고, sage green 3색 팔레트를 적용.

### 팔레트

| 상태 | 색상 |
|------|------|
| 비활성 | `#B5D98A` |
| 활성 | `#4A8E22` |
| 활성 stroke-width | `2.3` (비활성 `1.9`보다 굵게) |

### 아이콘 목록

| 탭 | 아이콘 컴포넌트 | 형태 |
|----|---------------|------|
| 영양제 | `IconLeaf` | 잎 + 중앙 줄기 |
| 복용약 | `IconPill` | 알약 반반 캡슐 |
| 음수량 | `IconDrop` | 물방울 |
| 내 일정 | `IconCalendar` | 달력 + 체크 |
| 설정 | `IconGear` | 톱니바퀴 |

모두 `stroke="currentColor"`, `fill="none"`, `strokeLinecap="round"`, `strokeLinejoin="round"` 적용.

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/NavBar.jsx` | 이모지 제거, SVG 아이콘 컴포넌트 5종 구현 |
| `src/components/NavBar.css` | 활성/비활성 색상, stroke-width, safe area 패딩 적용 |

---

## 2026-06-19 · Phase 3.8 — PWA 업데이트 감지 배너 구현

### 기능 개요

새 서비스 워커가 배포되면 앱이 자동으로 감지하고 하단에 "새 버전이 있습니다. 새로고침 하시겠어요?" 배너를 표시. 사용자가 확인하면 새 SW가 활성화되고 앱이 새로고침됨.

### 구현 흐름

```
새 SW 배포
  → SW install (waiting 상태 유지, skipWaiting 없음)
  → registration.onupdatefound 감지
  → window.dispatchEvent('swUpdateAvailable', registration)
  → App.jsx에서 swReg 상태 업데이트
  → <UpdateBanner> 렌더링
  → 사용자 "새로고침" 클릭
  → reg.waiting.postMessage({ type: 'SKIP_WAITING' })
  → 새 SW activate → controllerchange → window.location.reload()
```

### 추가된 파일

| 파일 | 역할 |
|------|------|
| `src/components/UpdateBanner.jsx` | 업데이트 알림 배너 컴포넌트 |
| `src/components/UpdateBanner.css` | 슬라이드업 애니메이션, NavBar 위 고정 |

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `public/service-worker.js` | install에서 `skipWaiting()` 제거, `SKIP_WAITING` 메시지 리스너 추가 |
| `src/main.jsx` | `swUpdateAvailable` 커스텀 이벤트 발행 |
| `src/App.jsx` | `swReg` 상태 + 이벤트 리스너 + `<UpdateBanner>` 렌더링 |

### 배너 UI

- 위치: NavBar 바로 위 (`bottom: calc(var(--nav-height) + env(safe-area-inset-bottom) + 12px)`)
- 버튼: "나중에" (dismiss) / "새로고침" (확인, 초록 강조)
- 애니메이션: `slideUp 0.3s ease-out`

---

## 2026-06-19 · Phase 3.9 — 탭바 iOS Safe Area 대응

### 문제

아이폰의 홈 인디케이터 바 위로 탭 아이콘이 겹쳐 보임. 탭바 실제 높이(1.3cm)가 레퍼런스 앱(1.55cm)보다 작음.

### 해결

`viewport-fit=cover`를 메타태그에 추가하고 CSS `env(safe-area-inset-bottom)`으로 홈 인디케이터 영역만큼 추가 패딩 확보.

### 변경된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `index.html` | `viewport-fit=cover` 추가 |
| `src/index.css` | `--nav-height: 62px` (콘텐츠 높이 기준; safe area는 별도 추가) |
| `src/components/NavBar.css` | 높이 `calc(var(--nav-height) + env(safe-area-inset-bottom, 0px))`, `padding-bottom: env(safe-area-inset-bottom, 0px)` |
| `src/App.css` | `padding-bottom: calc(var(--nav-height) + env(safe-area-inset-bottom, 0px) + 8px)` |

### 결과

| 기기 | 탭바 높이 |
|------|----------|
| 일반 Android/구형 iPhone | `62px` |
| Face ID iPhone (홈바 있음) | `62px + 34px ≈ 96px` ≈ 1.55cm |

---

## 2026-06-19 · Phase 3.10 — 흰 화면 근본 원인 2차 수정

### 남은 문제

Phase 3.5에서 SW 전략을 수정했음에도 특정 상황에서 흰 화면이 재발. 두 가지 추가 원인 발견.

### 원인 1: `controllerchange` 무한 루프

SW `activate` 시 `clients.claim()`이 호출되면 `controllerchange` 이벤트가 발생. 기존 코드는 이를 항상 `window.location.reload()`로 처리 → 첫 설치 시 무한 새로고침 루프.

**수정 (`src/main.jsx`):**

```js
// SW 등록 전에 현재 컨트롤러 유무를 저장
const hadController = !!navigator.serviceWorker.controller

let refreshing = false
navigator.serviceWorker.addEventListener('controllerchange', () => {
  // 기존 컨트롤러가 있던 경우(=업데이트)에만 새로고침
  if (!refreshing && hadController) {
    refreshing = true
    window.location.reload()
  }
})
```

### 원인 2: `supabaseClient.js` throw

Supabase 환경변수 누락 시 `throw new Error()`로 앱 전체가 크래시됨.

**수정 (`src/supabaseClient.js`):**

```js
// throw → console.error + placeholder URL로 graceful degradation
if (!supabaseUrl || !supabaseAnonKey) {
  console.error('[NutriTime] Supabase env vars missing — auth disabled')
}
export const supabase = createClient(
  supabaseUrl || 'https://placeholder.supabase.co',
  supabaseAnonKey || 'placeholder-anon-key'
)
```

### CACHE_NAME 버전

`nutritime-v5`로 업데이트 (구버전 캐시 강제 삭제).

---

## 2026-06-19 · Phase 4.0 — 헤더 UI 전면 정리

### 작업 배경

5개 탭 헤더의 크기와 여백이 각기 달라 탭 전환 시 레이아웃이 불안정. 4단계 작업으로 완전히 통일.

---

### Step 1: 검색바 제거 + 음수량 기준 높이 통일 (`79d0fc3`)

- 영양제·복용약 헤더의 검색바(약 이름/효능 검색) 제거
  - `LibraryPage.jsx`: `search` 상태, `filtered` 배열, 검색 div 제거 → `allSupplements.map` 직접 사용
  - `MedicationPage.jsx`: 동일 방식 적용
- 음수량 탭 헤더 크기를 기준으로 5개 탭 패딩 맞춤 조정

### Step 2: 내 일정 헤더 분리 (`4ce9d43`)

내 일정 탭의 진행률 바(progress bar)가 헤더 div 안에 있어 다른 탭보다 높이가 컸음.  
진행률 바를 헤더 밖 `.schedule-progress-card`로 이동.

```jsx
// 변경 전: 헤더 안에 progress bar 포함
// 변경 후: 헤더는 타이틀 + 서브텍스트만, progress bar는 별도 카드
<div className="schedule-header">
  <h1 className="schedule-title">📋 내 일정</h1>
  <p className="schedule-sub">영양제와 약을 꾸준히 복용하세요</p>
</div>
<div className="schedule-progress-card">
  {/* progress bar */}
</div>
```

### Step 3: 위아래 여백 균등화 (`3082f62`)

음수량 탭 헤더 이미지 분석 결과, 텍스트 블록 위 여백(28px)과 아래 여백(50px)이 불일치.  
5개 탭 모두 `padding: 28px 20px 28px`으로 통일.

### Step 4: 영양제·복용약 subtitle 마진 제거 (`54119f7`)

영양제·복용약 CSS의 `.library-sub`, `.medication-sub`에 `margin-bottom: 16px`이 남아 있어 다른 3개 탭보다 헤더가 여전히 높았음. 해당 마진 제거.

### 최종 헤더 구조 (5탭 공통)

```
padding: 28px 20px 28px
├── <h1>  타이틀 (font-size: 22px, font-weight: 800)   margin-bottom: 6px
└── <p>   서브텍스트 (font-size: 13px)                  (no margin)
```

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 4.0 · 헤더 UI 통일 완성*

---

## 2026-06-20 · Phase 4.1 — 탭 서브텍스트 변경 + 복용약 데이터 정리

### 탭 헤더 서브텍스트 변경 (`c969745`)

| 탭 | 변경 전 | 변경 후 |
|----|---------|---------|
| 영양제 | 영양제를 탭하면 상세 정보를 확인할 수 있어요 | 오늘보다 내일 더 건강해지세요 |
| 복용약 | 약을 탭하면 복용법과 주의사항을 확인할 수 있어요 | 잊지말자 꼬박꼬박 |
| 음수량 | 권장 음수량을 추적하세요 | 커피도 좋지만 물도 마시자 |

### 복용약 데이터 정리 (`cc51166`)

| 파일 | 삭제 항목 | 변경 종수 |
|------|-----------|-----------|
| `src/data/medications.js` | 관절염약, 통풍약 | 11종 → **9종** |

**현재 복용약 목록 (9종)**
갑상선약 · 고지혈증약 · 당뇨약 · 수면제 · 아스피린 · 위산억제제 · 항우울제 · 혈압약 · 협심증약

---

## 2026-06-20~22 · Phase 4.2 — 카카오 연동 시도 및 전면 롤백

### 작업 배경

복용 알림을 Web Push 대신 카카오톡 메시지로 발송하는 기능을 시도. GitHub Pages는 서버리스 환경이므로 Supabase Edge Function을 백엔드로 활용하는 구조로 설계.

---

### 구현 내역

#### 생성 파일

| 파일 | 역할 |
|------|------|
| `src/lib/kakaoNotification.js` | Kakao SDK 초기화·로그인·로그아웃·토큰 조회 래퍼 |
| `src/components/KakaoLoginButton.jsx` | Kakao 로그인/로그아웃 UI 컴포넌트 |
| `src/components/NotificationPanel.jsx` | 알림 테스트 발송 UI |
| `src/context/AuthContext.jsx` | Supabase 세션 기반 `useAuth()` 훅 |
| `src/lib/supabase.js` | `supabaseClient.js` re-export |
| `supabase/functions/send-kakao-notification/index.ts` | Supabase Edge Function (Kakao API 호출 + 로그 기록) |

#### Supabase 테이블 (대시보드에서 생성)

```sql
-- 카카오 액세스 토큰 저장
create table kakao_tokens (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade,
  access_token text not null,
  expires_at timestamptz,
  unique(user_id)
);

-- 알림 발송 이력
create table notification_logs (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade,
  title text, body text not null, link text,
  channel text default 'kakao',
  success boolean, response_code int, error_message text,
  sent_at timestamptz default now()
);
```

#### 설정 탭 UI 재구성

`카카오 연동` 섹션을 별도 분리 → `계정` 섹션 안에 Google·Kakao 통합.

- Google: 연결됨(프로필 + 연결해제) / 미연결(Google로 연결 버튼)
- Kakao: 연결됨(프로필 + 연결해제) / 미연결(Kakao로 연결 버튼, 노란 배경)
- Kakao 연결 시 `카카오 알림` 섹션 노출 → `NotificationPanel` 표시

---

### 트러블슈팅

#### KOE205 오류 — "Not allowed URI"

| 원인 | Kakao 앱에 사이트 도메인·Redirect URI 미등록 |
|------|------|
| 1차 대응 | Kakao Developers Console → 플랫폼 Web 등록, Redirect URI 추가 |
| 2차 대응 | `loginWithKakao` 구현 버그 수정 — `Kakao.Auth.authorize`(페이지 리다이렉트)를 `Kakao.Auth.login`(팝업)으로 교체 |

**버그 원인**: `authorize`는 브라우저를 Kakao 페이지로 이동시키므로 이후 `setTimeout`이 실행되지 않음. 팝업 방식은 콜백으로 즉시 토큰을 수신하며 Redirect URI 등록이 불필요.

```js
// 변경 전 — authorize + setTimeout (동작 안 함)
window.Kakao.Auth.authorize({ redirectUri: window.location.href, scope: '...' })
setTimeout(() => { /* 이 시점엔 이미 다른 페이지 */ }, 1000)

// 변경 후 — login 팝업 (정상 동작)
window.Kakao.Auth.login({
  scope: 'profile_nickname,account_email',
  success: (authObj) => resolve(authObj.access_token),
  fail: (err) => reject(new Error(err.error_description)),
})
```

---

### 롤백 결정

Console 등록 및 코드 수정 후에도 Kakao 로그인 연동이 안정적으로 동작하지 않아 전체 제거 결정.

#### 삭제 내역 (`28c5b4e`)

| 항목 | 내용 |
|------|------|
| 파일 6개 삭제 | KakaoLoginButton, NotificationPanel, AuthContext, kakaoNotification, supabase.js, Edge Function |
| `index.html` | Kakao SDK 스크립트 제거 |
| `src/main.jsx` | `AuthProvider` 래퍼 제거 |
| `SettingsPage.jsx` | 카카오 연동 섹션·알림 설정 섹션 제거, Google 로그인만 유지 |

#### 롤백 후 설정 탭 구조

```
⚙️ 설정
├── ⏰ 나의 하루 일정  (시간 설정 5개)
├── 👤 계정           (Google 로그인/로그아웃)
└── ℹ️ 앱 정보
```

---

### 배포 URL

> **https://alicelimti.github.io/NutriTime/**

---

*현재 버전: Phase 4.2 · 카카오 연동 시도 및 롤백 — Google 로그인만 유지*
